# 비워 두면 /api 로 요청하고 vite.config.js의 프록시가 localhost:8080으로 넘겨 준다(로컬 개발 권장).
# 배포 환경에서만 백엔드 주소를 직접 지정한다. 이 값을 쓰면 CORS를 타므로
# 백엔드의 CORS_ALLOWED_ORIGINS에 프론트엔드 도메인을 반드시 등록해야 한다.
# VITE_API_BASE_URL=https://wakebook-api.example.com/api
